pre-commit:
  commands:
    format:
      glob: '*.js'
      run: npx biome check --write --no-errors-on-unmatched {staged_files}
      stage_fixed: true

pre-push:
  commands:
    test:
      run: node --test 'test/**/*.js'
